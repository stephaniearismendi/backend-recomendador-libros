/**
 * DTO layer exports
 * Centralized exports for all DTO functions
 */

const UserDTO = require('./UserDTO');
const BookDTO = require('./BookDTO');

module.exports = {
    UserDTO,
    BookDTO,
};
