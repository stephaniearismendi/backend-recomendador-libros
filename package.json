{
  "name": "backend-tfg",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "init:users": "node scripts/init-users.js",
    "init:users:force": "node scripts/init-users.js --force",
    "init:users:clear": "node scripts/init-users.js --clear",
    "init:posts": "node scripts/init-posts.js",
    "init:posts:force": "node scripts/init-posts.js --force",
    "init:posts:clear": "node scripts/init-posts.js --clear",
    "db:reset": "prisma migrate reset",
    "db:studio": "prisma studio",
    "lint": "eslint src/ tests/",
    "lint:fix": "eslint src/ tests/ --fix",
    "format": "prettier --write src/ tests/",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": [
    "nodejs",
    "express",
    "prisma",
    "api",
    "backend"
  ],
  "author": "Stephanie",
  "license": "ISC",
  "description": "Backend API for TFG project - Book social network",
  "dependencies": {
    "@prisma/client": "^6.10.0",
    "axios": "^1.10.0",
    "bcryptjs": "^3.0.2",
    "bottleneck": "^2.19.5",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-request-id": "^3.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "p-retry": "^7.0.0",
    "pino": "^9.9.0",
    "pino-http": "^10.5.0",
    "pino-pretty": "^13.1.1",
    "prisma": "^6.10.0"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.9.0",
    "cross-env": "^10.0.0",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.1.0",
    "prisma-erd-generator": "^2.0.4",
    "supertest": "^6.3.3"
  }
}
